# ═══════════════════════════════════════════════════════════════════════════
# 🍝 Spaghetti Migrator - Environment Configuration
# Untangle your nested folders and migrate assets between cloud services
# ═══════════════════════════════════════════════════════════════════════════

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ MIGRATION ENVIRONMENT                                                   │
# └─────────────────────────────────────────────────────────────────────────┘
# Options: dev, staging, prod
# Start with 'dev' for testing, then move to 'staging', then 'prod'
MIGRATION_ENV=dev

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ DIGITALOCEAN SPACES CONFIGURATION (REQUIRED)                            │
# └─────────────────────────────────────────────────────────────────────────┘

# DigitalOcean Spaces Access Key
# Where to find: DigitalOcean Control Panel → API → Spaces Keys
DO_S3_ACCESS_KEY=your_spaces_access_key_here

# DigitalOcean Spaces Secret Key
# Where to find: DigitalOcean Control Panel → API → Spaces Keys
DO_S3_SECRET_KEY=your_spaces_secret_key_here

# DigitalOcean Spaces Bucket Name
# Example: my-craft-assets
DO_S3_BUCKET=your-bucket-name

# DigitalOcean Spaces Base URL (Public URL for assets)
# Format: https://{bucket-name}.{region}.digitaloceanspaces.com
# Example: https://my-craft-assets.tor1.digitaloceanspaces.com
DO_S3_BASE_URL=https://your-bucket.tor1.digitaloceanspaces.com

# DigitalOcean Spaces Endpoint (API Endpoint - NO bucket name)
# Format: https://{region}.digitaloceanspaces.com
# Example: https://tor1.digitaloceanspaces.com
# Available regions: nyc3, ams3, sgp1, sfo3, fra1, tor1
DO_S3_BASE_ENDPOINT=https://tor1.digitaloceanspaces.com

# DigitalOcean Spaces Region
# Common values: nyc3, ams3, sgp1, sfo3, fra1, tor1
DO_S3_REGION=tor1

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ AWS S3 CONFIGURATION (Source - Optional)                                │
# └─────────────────────────────────────────────────────────────────────────┘
# These are optional if you configure them directly in config/migration-config.php
# Alternatively, you can use environment variables for AWS as well

# AWS S3 Bucket Name (source bucket)
# AWS_S3_BUCKET=your-aws-bucket

# AWS S3 Region
# AWS_S3_REGION=us-east-1

# AWS Access Key (if not using IAM roles)
# AWS_ACCESS_KEY_ID=your_aws_key

# AWS Secret Key (if not using IAM roles)
# AWS_SECRET_ACCESS_KEY=your_aws_secret

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ MIGRATION SETTINGS (Optional - Defaults provided in config)            │
# └─────────────────────────────────────────────────────────────────────────┘

# Batch size for processing assets (default: 100)
# MIGRATION_BATCH_SIZE=100

# Checkpoint frequency - save progress every N items (default: 50)
# MIGRATION_CHECKPOINT_FREQUENCY=50

# Maximum retry attempts for failed operations (default: 3)
# MIGRATION_MAX_RETRIES=3

# Retry delay in seconds (default: 5)
# MIGRATION_RETRY_DELAY=5

# Enable dry run mode globally (default: false)
# MIGRATION_DRY_RUN=false

# Enable verbose logging (default: false)
# MIGRATION_VERBOSE=true

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ CRAFT CMS CONFIGURATION (Standard Craft env vars)                      │
# └─────────────────────────────────────────────────────────────────────────┘

# Craft environment (dev, staging, production)
CRAFT_ENVIRONMENT=dev

# Security key (generate with: ./craft setup/security-key)
CRAFT_SECURITY_KEY=

# Database configuration
CRAFT_DB_DRIVER=mysql
CRAFT_DB_SERVER=127.0.0.1
CRAFT_DB_PORT=3306
CRAFT_DB_DATABASE=craft_db
CRAFT_DB_USER=craft_user
CRAFT_DB_PASSWORD=craft_password
CRAFT_DB_SCHEMA=public
CRAFT_DB_TABLE_PREFIX=

# Site URL
PRIMARY_SITE_URL=https://your-site.com

# ═══════════════════════════════════════════════════════════════════════════
# NOTES:
# ═══════════════════════════════════════════════════════════════════════════
#
# 1. NEVER commit the actual .env file to version control
# 2. Copy this file: cp .env.example .env
# 3. Update values in your .env file with real credentials
# 4. After configuration, run: ./craft spaghetti-migrator/migration-check/check
# 5. Keep your credentials secure and rotate them regularly
#
# For detailed setup instructions, see:
# https://github.com/csabourin/do-migration#readme
