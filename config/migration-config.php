<?php
/**
 * Default configuration shipped with the NCC migration module.
 *
 * The full interactive template lives in `console/controllers/config_examples`.
 * Keeping this file as a thin wrapper allows developers to copy a single file
 * into their Craft `config/` directory while still benefitting from the richly
 * documented example that ships with the toolkit.
 */

return require __DIR__ . '/../console/controllers/config_examples/migration-config.php';
